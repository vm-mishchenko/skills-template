# Example command

A prompt you trigger by typing `/example-command`. The whole file is the prompt, so write it as
instructions. Use a command when you decide to invoke it, a skill when the agent should decide.
Link to `references/` instead of pasting long templates inline.

Delete this file and add your own.

## Summarize the repository

1. Identify the entry points and the main directories.
2. List the external dependencies and what each is used for.
3. Report anything that looks unfinished or inconsistent.
4. Write it up using [example-template.md](../references/example-template.md).

Ask the user before making any change.
